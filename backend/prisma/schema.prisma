// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model WorkStations {
  id             Int              @id @default(autoincrement())
  sector         String
  isDisposal     Boolean          @default(false)
  qrcode         String
  description    String?
  createdAt      DateTime         @default(now()) @updatedAt
  Paths          Paths[]          @ignore
  DefectivePaths DefectivePaths[] @ignore

  @@map("workstations")
}

model Users {
  id           String   @id @default(uuid())
  name         String
  password     String
  email        String   @unique
  isSupervisor Boolean  @default(false)
  createdAt    DateTime @default(now()) @updatedAt

  @@map("users")
}

model Products {
  SN        String   @id
  createdAt DateTime @default(now()) @updatedAt
  Paths     Paths[]  @ignore

  @@map("products")
}

model DefectiveProducts {
  id             Int              @id @default(autoincrement())
  createdAt      DateTime         @default(now()) @updatedAt
  DefectivePaths DefectivePaths[] @ignore

  @@map("defective_products")
}

model Trays {
  id        Int      @id @default(autoincrement())
  qrcode    String
  createdAt DateTime @default(now()) @updatedAt

  @@map("trays")
}

model Paths {
  stationId Int
  prodSN    String
  createdAt DateTime @default(now()) @updatedAt

  workStation WorkStations @relation(fields: [stationId], references: [id]) @ignore
  product     Products     @relation(fields: [prodSN], references: [SN]) @ignore

  @@id([stationId, prodSN])
  @@map("paths")
}

model DefectivePaths {
  stationId Int
  defProdId Int
  createdAt DateTime @default(now()) @updatedAt

  workStation      WorkStations      @relation(fields: [stationId], references: [id]) @ignore
  defectiveProduct DefectiveProducts @relation(fields: [defProdId], references: [id]) @ignore

  @@id([stationId, defProdId])
  @@map("defective_paths")
}
